# Electron mirrors + native module handling for pnpm's isolated store.
# Public hoist pattern keeps Electron toolchain resolvable across the workspace.
public-hoist-pattern[]=*electron*
public-hoist-pattern[]=*vite*
strict-peer-dependencies=false

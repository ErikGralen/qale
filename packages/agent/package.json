{
  "name": "@qale/agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "comment:exports": "The agent package is main-only except for one subpath. session-stats.ts is for scripts/session-stats.ts, run under plain node outside Electron; it reads a pi session file and reports what a run cost. Its own subpath keeps it out of the Electron main bundle, which imports \"@qale/agent\" for the runtime and does not need a stats parser riding along.",
  "exports": {
    ".": "./src/index.ts",
    "./session-stats": "./src/session-stats.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@qale/application": "workspace:*",
    "@qale/connectors": "workspace:*",
    "@qale/domain": "workspace:*",
    "@qale/sessions": "workspace:*",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-ai": "0.83.0",
    "ai": "^7.0.22",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@qale/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.1",
    "typescript": "~5.9.2"
  }
}

{
  "name": "@qale/agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "comment:exports": "The slot parser is the one exception to \"the agent package is main-only\". The renderer has to draw a box exactly where the fence is (docs/brainstorm-skill.md), and two scanners would drift; this subpath is that one file, which imports nothing, so nothing else in here reaches the browser bundle.",
  "exports": {
    ".": "./src/index.ts",
    "./slots": "./src/slots.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit -p tsconfig.json",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@qale/application": "workspace:*",
    "@qale/connectors": "workspace:*",
    "@qale/domain": "workspace:*",
    "@qale/sessions": "workspace:*",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-ai": "0.83.0",
    "ai": "^7.0.22",
    "typebox": "1.3.7"
  },
  "devDependencies": {
    "@qale/typescript-config": "workspace:*",
    "@types/node": "^24.0.0",
    "tsx": "^4.23.1",
    "typescript": "~5.9.2"
  }
}

---
type: skill
skill_kind: session
session_type: broken-demo
summary: Deliberately broken bindings — shows how the Skills view surfaces frontmatter errors
bindings:
  - mode: whenever
    event: capture.transcript
  - mode: triggered
    event: coffee.brewed
---

## When
Never. This file exists only to demonstrate how invalid bindings and autonomy surface on the
Skills page — pinned in the flag voice, so a broken skill can never fail silently.
